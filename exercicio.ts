function calculaMultiplicacao(numero1: number, numero2: number): number {
    return numero1 * numero2;
}

const calculaMultiplicacao2 = (numero1: number, numero2: number): number => numero1 * numero2;



function saudacao(nome: string): string {
    return `Olá ${nome}`;
}

const saudacao2 = (nome: string): string => `Olá ${nome}`;